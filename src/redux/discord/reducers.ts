import ACTION from './actionsType'

const initialState = {}

const discord = (state = initialState, action: { type: string; payload?: object }) => {
  switch (action) {
    default:
      return state
  }
}

export default discord
